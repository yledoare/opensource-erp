version: '3'

networks:
    internal-pod:
        internal: true
    external-pod:
        internal: false

services:
    dolidb:
        image: mariadb:latest
        environment:
            MYSQL_ROOT_PASSWORD: root
            MYSQL_DATABASE: dolibarr
        ports:
            - "3306:3306"
        networks:
            - internal-pod
            - external-pod

    doliweb:
        build: .
        environment:
            HOST_USER_ID: $HOST_USER_ID
        volumes:
            - ../../dolibarr-17.0.1/htdocs:/var/www/html/
            - ../../documents:/var/documents
        ports:
            - "8029:80"
            - "9000:9000"
        depends_on:
            - dolidb
        networks:
            - internal-pod
            - external-pod
